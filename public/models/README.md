Place your GLB device files here using these filenames:

- iphone-16-pro.glb
- iphone-16-pro-max.glb
- ipad-pro-13.glb
- ipad-mini.glb
- macbook-pro-14.glb

Each model should expose a screen mesh named either `Screen` or `Display`,
matching the `screenMeshName` field in `lib/devices.ts`.
